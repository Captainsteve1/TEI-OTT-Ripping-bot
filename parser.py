import os
import requests
import index

index = index.Zee5
index.login()
index.manifest()

class Parser:
      def __init__():
        self.id = getattr(index , 'id')
        self.mpd = getattr(index , 'mpd_url')
        
    def parse():
        